# Supabase — client-side (VITE_ prefix required for Vite build-time replacement)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Supabase — server-side (server functions, SSR, service role)
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Gemini AI (Google Generative Language API)
# Get your key at https://aistudio.google.com/apikey
# Used for chat, memory extraction, check-ins, and wonder reports.
# Model: gemini-2.0-flash via Google's OpenAI-compatible endpoint.
GEMINI_API_KEY=
